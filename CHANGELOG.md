# 更新日志

## v0.8.1（2026-08-01）

- 发布前交叉审查修复（小花 + 绯月 + 柚月）：
  - 休息窗口结算期守卫：休息结束后点击跳过 / 按 Escape / 按钮追逃不再生效，防止已完成结算被覆盖成逃逸
  - 配置写入改原子写（config.json / model-config.json），断电或崩溃不再留下半截 JSON
  - 模型配置保存改用校验后的配置落盘，校验与写入保持一致
  - TESTING.md 分享版边界说明同步到三档模型配置
  - 三击逃生窗口时长文档与代码对齐（650ms）
- README 改版：重写「它是干什么的」、花招玩法不剧透、逃生窗口彩蛋、新增致谢 stretchly

## v0.8.0（2026-08-01）

- 恢复模型配置（分享版标准）：文案模型行不再是只读标签，点「配置」打开弹窗
- 三档使用方式：跟随当前助手（默认）/ 从 Hana 已配置模型中选择（拉取列表，不碰供应商密钥）/ 自定义 API（地址 + Key + 模型名）
- 「测试连通」按钮：发送一条短消息验证所选模型真实可用，测试不保存
- API Key 脱敏：页面只显示占位符，留空保存不会覆盖已存 Key，明确清除才删除
- 自定义配置只保存在插件数据目录，不随插件代码分发
- manifest 恢复 network.fetch 权限（自定义 API 直连用）

## v0.7.0（2026-08-01）

- 全屏时暂停：看视频、玩游戏全屏时计时冻结，不弹 10 秒提醒、不弹全屏休息窗口，退出全屏后接着算
- 暂离暂停：超过设定时间没动鼠标键盘（默认 5 分钟），判定走开，计时跟着暂停，回来继续
- 到点兜底升级：检测到全屏/免打扰时不再"跳过本轮休息"，改为原地等待，环境恢复后再补上这次休息（留 10 秒缓冲）
- 新增设置：全屏暂停开关、暂离暂停开关、暂离判定时长（3/5/10 分钟可自定义）
- 状态栏与设置页顶部会显示"全屏中/暂离中，计时暂停"
- 环境检测每 5 秒一次，识别延迟最多 5 秒

## v0.6.1（2026-08-01）

- 工作间隔与休息时长的最后一个选项改为「自定义」：点击后变成带单位提示的输入框（数字 + 分钟），回车或鼠标点别处即可保存，Esc 取消
- 休息时长预设调整为 20 秒 / 30 秒 / 1 分钟；支持小数分钟输入（如 1.5 = 1 分 30 秒），自动换算存储
- 旧的非预设配置（如 45 分钟 / 5 分钟）自动显示在自定义选项上并保持选中，无需重新设置
- 输入防呆：非法内容静默还原，工作间隔上限 480 分钟、休息时长上限 120 分钟

## v0.6.0（2026-07-31）

- 分享版清理：移除使用者姓名、固定助手名单、私人路径与身份署名
- 当前助手的名称、身份和头像每次打开窗口前重新读取，适配新增、删除、改名和换头像
- 文案生成统一走 Hana `utility:call-text`，按当前 `agentId` 与 `sessionPath` 自动使用该助手已配置的模型
- 移除独立模型配置、自定义 API、API Key 存储和任意网络权限
- 删除设置页“立即测试全屏提醒”按钮与对应页面 API，重新整理底部布局
- 本地兜底改为不绑定任何助手或使用者的通用文案
- 成就系统更新为 10 枚一次性成就 + 4 枚四阶段成就

## v0.5.11（2026-07-31）

- 修复强制模式下滑动/回复触发 Qt6Core.dll 原生崩溃（`main_layout.activate()` 在部分环境手动激活布局会导致崩溃，改为交给事件循环自动重排）
- 修复回复错误状态的显示（新增 `_last_reply_error` 状态跟踪）

## v0.5.10（2026-07-31）

- 修复欠账提示在窗口初始化阶段触发 Qt 原生崩溃导致窗口秒退

## v0.5.9（2026-07-31）

- 长文案修复：文本换行后按真实宽度重算高度，不再被压缩裁切
- 写死兜底文案与本地占位文案本轮内不重复，全用完后才循环

## v0.5.8（2026-07-31）

- 停滞期间点击直接忽略，花招池不被提前洗牌
- emoji 三击逃生加 `_finished` 守卫，休息结束后不可触发
- 象限落点 margin 调整，窄屏下落点范围不再变窄
- 动画状态检查补 Paused，防止暂停态漏网

## v0.5.7（2026-07-31）

- 追击边界清晰化：一次 move 花招 = 主动逃一次 + 追击最多再逃一次（固定 2 次）
- 追击再逃时文案明说"还敢追？那就再跑一次给你看"
- 追加次数用完立即结束追击窗口；滑回等待 2.5s → 1.8s

## v0.5.6（2026-07-31）

- 追击窗口机制：划走只在抽中 move 花招后开启 1.8 秒追击窗口，滑动不再每次点击后乱入
- 没收时清掉追击窗口，恢复后不残留追逃状态

## v0.5.5（2026-07-31）

- 停滞可视化：倒计时数字上盖半透明红叉 + 数字变灰，一眼看出时间停了

## v0.5.4（2026-07-31）

- 划走动画提速：240ms → 150ms，逃跑更果断
- 划走动画运行中点击完全无效，追到的都是空气

## v0.5.3（2026-07-31）

- 进度条按"初始时长 + 已加时"计算比例，加时后正常倒退不再顶格
- 逃生出口改为三击情绪 emoji（650ms 窗口判定）

## v0.5.2（2026-07-31）

- 花招冲突修复：划走动画不再被打断；没收时先停动画再藏按钮，变成"跑一半被抓走"
- 情绪 emoji 在最终效果确定后再设，降级为普通文案时表情同步回默认

## v0.5.1（2026-07-31）

- 点击跳过不再永远取消回中心，停手后按钮仍会自动滑回
- 划走落点改为象限随机 + 落点随机；每次窗口划走次数随机
- 情绪气泡位置修正，完全放在头像容器内

## v0.5.0（2026-07-31）

- 新花招：按钮没收（消失 1.5 秒回到中心重新出现）
- 加时升级为倒计时倒退，进度条同步倒退并闪红
- 新增休息进度条
- 新增情绪 emoji 气泡（😊😤😏🤨🫣）
- 新增没收类型文案全套
- 统计口径：斗智斗勇时长 = 点击次数 × 10 秒
- 滑回中心后感应圈休眠 2 秒

## v0.4.2（2026-07-31）

- 红框呼吸效果真正生效（动画强度接入透明度计算）
- 预生成池抽走即移除，展示过的文案本轮不重复
- 台词档案加"本轮已抽"标记，新窗口重置

## v0.4.1（2026-07-31）

- 开场完全正常：按钮居中、放松姿势照常显示，第一次点击跳过后花招才开始
- 划走改为点击抽签触发，不再被鼠标靠近常驻消耗；结束后自动滑回
- 红框改呼吸式：统一两下，亮 420ms / 暗 650ms 慢速渐变

## v0.4.0（2026-07-31）

- 花招体系：普通/划走/加时/停滞四类文案，预生成按类型
- 台词档案：模型台词沉淀（每类限 40 条），越用越活
- 休息期间每 2 分钟自动补充新文案
- 按钮划走：150px 感应圈 + 滑动动画，追 3 次封顶
- 花招抽签池：一轮不重复，用完洗牌
- 倒计时停滞 3~5 秒
- 欠账：点跳过记 10 秒（上限 120 秒），下次窗口补还
- 成就：10 个成就 + 6 级称号 + 三个统计指标
- 结算：完成休息发结算请求，展示夸夸与成就
- 红框闪烁 2~3 下

## v0.3.0（2026-07-31）

- 休息前按当前助手性格和最近对话预生成劝休息文案
- 点击即时反馈，不再同步等待模型
- 当前助手自动识别（名字 + 头像）
- 文件 IPC 桥：Python 点击写请求，Node 回写响应
- 设置页改版为暖色手帐风

## v0.2.0（2026-07-31）

- 全屏强制休息窗口基础版
- 环境检测（全屏应用 / 免打扰时跳过休息）
- 欠账提示基础实现

## v0.1.0（2026-07-19）

- 基础功能：定时提醒 + 全屏休息窗口（PyQt6）
- 普通模式可跳过，强制模式点击跳过触发劝阻
