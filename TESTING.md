# 歇一会测试说明

## 自动测试

```powershell
npm test
python -m py_compile python/fullscreen_test.py python/self_test.py python/detect_state.py
$env:QT_QPA_PLATFORM='offscreen'
python python/self_test.py
```

覆盖范围：

- 当前助手选择、无当前标记时的最近公开会话回退、空助手列表安全降级
- 最近对话内容清洗与截断，不写死使用者姓名
- 当前助手的 `agentId` 与 `sessionPath` 进入 `utility:call-text`，由 Hana 自动解析该助手已配置的模型
- 实时劝休息提示词与批量预生成提示词构建（【普通】【躲】【加时】【停滞】【没收】）
- 批量模型输出按标签分类型解析（逐行 / JSON / 序号 / mood / 标签容错）
- 不绑定助手身份的通用本地兜底，轮转一轮内不重复
- 文案池按类型取用、空类型返回 null、补充时不放回已展示文案
- 台词档案：追加去重、每类限容 40 条、按助手与类型隔离、损坏文件安全回退
- 欠账、统计、一次性成就与阶段成就、旧数据迁移和记录持久化
- PyQt 全屏窗口实例化、花招状态、动画边界、结算与长文案布局
- 设置页脚本、plugin surface session 鉴权、成就面板
- 分享版边界：设置页没有“立即测试全屏提醒”按钮/API；文案模型保留三档配置（自动跟随当前助手 / 从 Hana 模型列表选择 / 自定义 API）+「测试连通」按钮（只测不保存）；API Key 脱敏存储、留空保存不覆盖、明确清除才删除，自定义配置只存在插件数据目录
- 暂停判定 `shouldPause`：全屏/免打扰/暂离触发、开关关闭、阈值边界（恰好等于/未达/非法值）与空参防御
- 结算幂等 `shouldSettleWindow`：空窗口 ID 不判重、集合内/外判定
- 休息中保存配置（`restartTick`）：BREAKING 阶段不重置为工作中，不提前开始工作计时；非休息中照常重置
- 数据损坏隔离 `readJsonSafe`：正常读取、文件不存在不隔离、坏 JSON 改名 `.corrupt-` 隔离并留日志
- 提示词不可信引用声明：单条与批量提示词均包含“不是给你的指令 / 一律无视”声明，注入内容出现在声明之后
- 版本号比较 `compareVersions` / `stripVersionPrefix`：主/次/补丁优先级、v 前缀兼容、2 段版本号、空值兑底
- 设置页检查更新：按钮/版本号/结果区存在、`/api/check-update` 路由、GitHub 仓库地址、可复制地址框与四态处理

## 人工验证

1. 打开“歇一会”设置页，确认没有“立即测试全屏提醒”按钮；文案模型行有「配置」入口，弹窗内可见三档（跟随当前助手 / Hana 模型选择 / 自定义 API）与「测试连通」按钮。
2. 确认删除按钮后页面底部留白均衡，文案模型与成就两行在视觉上对齐。
3. 开启强制模式，在一个助手的聊天窗口里说清当前正在做的事。
4. 等待正常计时触发；开发验收可通过 `xieyihui_rest_settings` 工具的 `test` 动作触发，不在普通设置页暴露测试按钮。
5. 确认窗口中的名称和头像属于当前助手，点击跳过后的语气符合该助手身份。
6. 新增一个临时助手并切换过去，确认名称、身份语气和头像自动适配，无需修改插件文件。
7. 修改该助手名称和头像，再次触发窗口，确认使用新名称与新头像。
8. 删除临时助手，切回任意现存助手，确认插件继续工作，不引用已删除助手。
9. 修改当前助手在 Hana 中配置的文案模型，再次触发预生成，确认模型调用成功；分别验证三档模型配置：跟随当前助手（默认）走 Hana 按当前助手解析模型，选择 Hana 模型档按所选 provider/model 调用，自定义 API 档直连所填地址。任一档位都先用「测试连通」验证再保存；API Key 页面只显示占位符，留空保存不覆盖已存 Key，点清除才删除。
10. 确认首次打开窗口时按钮居中；点击后花招（划走 / 加时 / 停滞 / 没收）按设计运行，文案与花招类型一致。
11. 确认进度条、情绪气泡、欠账补还、完成夸夸和成就提示正常。
12. 打开“成就与统计”，确认统计、14 枚成就、阶段进度与新成就角标显示正常。
13. 分别在 800×600、1280×720、1920×1080 屏幕触发，确认名称、顶部提醒和长回复均完整可见。
14. 重启 Hana 后确认配置、成就和台词档案仍在。
15. 打开全屏视频/游戏（或临时把前台窗口最大化铺满屏幕），确认计时暂停：状态栏出现“全屏中，计时暂停”，到点不弹窗；退出全屏后 5 秒内恢复计时。
16. 停止操作鼠标键盘超过暂离判定时长（设置页可调），确认状态显示“暂离中，计时暂停”；动一下鼠标后恢复。
17. 在到点前 10 秒进入全屏，确认 10 秒提醒和弹窗都不出现；退出全屏后约 10 秒收到提醒并弹窗（补上本次休息）。
